import 'package:flutter/material.dart';

class TrashScanScreen extends StatelessWidget {
  const TrashScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("This is Trash Scan Screen")),
    );
  }
}
